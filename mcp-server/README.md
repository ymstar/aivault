# ⚠️ Moved

This MCP server has been extracted to a standalone repository:

**→ [aivault-org/aivault-mcp-server](https://github.com/aivault-org/aivault-mcp-server)**

The new version connects to AIVault via HTTP API (no direct database access needed).

```bash
npx aivault-mcp-server
```

See the [new README](https://github.com/aivault-org/aivault-mcp-server#readme) for setup instructions.
